name: Deploy (support-func)

on:
  workflow_dispatch: {}

jobs:
  deploy:
    name: Deploy
    uses: pagopa/dx/.github/workflows/function_app_deploy.yaml@main
    secrets: inherit
    with:
      workspace_name: io-func-sign-support
      environment: webapp-prod
      resource_group_name: io-p-sign-backend-rg
      function_app_name: io-p-sign-support-func
      health_check_path: /
      use_staging_slot: true