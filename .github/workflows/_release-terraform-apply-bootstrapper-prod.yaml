name: Release Bootstrapper Infrastructure - prod
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "infra/bootstrapper/prod/**"

permissions:
  contents: read
  id-token: write

jobs:
  release:
    uses: pagopa/dx/.github/workflows/release-terraform-bootstrapper-v1.yaml@e8fea7c190c031e6079c38e5cd204269d9ed4a6b # main
    name: Release Bootstrapper (prod)
    secrets: inherit
    with:
      environment: 'prod'
